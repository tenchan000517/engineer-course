# Engineer Course アプリ 引き継ぎドキュメント

**作成日**: 2025-10-25
**プロジェクト**: Next.js講座コンテンツのWebアプリ化
**プロジェクト名**: `engineer-course`
**場所**: `C:/portfolio-course/engineer-course/`

---

## 📋 プロジェクト概要

### 目的
マークダウン形式で作成された「Next.jsポートフォリオ制作講座」（全7モジュール）を、Next.jsのWebアプリケーションとして公開する。

### ターゲットデザイン
**Progate（プロゲート）風**のデザイン

#### デザイン要件
- ✅ カラフル・モダン
- ❌ グラデーション禁止（単色のみ使用）
- ❌ 絵文字禁止
- ✅ クリーンで読みやすい
- ✅ 左サイドバー（モジュール一覧）
- ✅ 大きめのボタン
- ✅ 完璧なレスポンシブ対応

---

## ✅ 完了済みの作業

### 1. プロジェクトセットアップ
```bash
# 実行済み
cd C:/portfolio-course
npx create-next-app@latest engineer-course --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"
```

**選択したオプション:**
- TypeScript: Yes
- Tailwind CSS: Yes
- ESLint: Yes
- App Router: Yes
- src directory: No
- Import alias: @/*
- React Compiler: No
- Turbopack: No

### 2. パッケージインストール
```bash
# 実行済み
cd C:/portfolio-course/engineer-course
npm install react-markdown remark-gfm rehype-highlight rehype-raw gray-matter
```

**インストールされたパッケージ:**
- `react-markdown`: マークダウンをReactコンポーネントとして表示
- `remark-gfm`: GitHub Flavored Markdown（テーブル、チェックボックスなど）
- `rehype-highlight`: コードブロックのシンタックスハイライト
- `rehype-raw`: HTML in Markdownのサポート
- `gray-matter`: YAMLフロントマターの解析

### 3. ディレクトリ構造作成
```bash
# 実行済み
mkdir -p content/modules components lib app/module/[slug]
```

### 4. マークダウンファイルのコピー
```bash
# 実行済み
cp module-*.md engineer-course/content/modules/
cp README.md engineer-course/content/
```

**コピーされたファイル:**
- `content/modules/module-01-environment-setup.md` (700行)
- `content/modules/module-02-nextjs-project-init.md` (650行)
- `content/modules/module-03-asset-preparation.md` (600行)
- `content/modules/module-04-basic-ui.md` (850行)
- `content/modules/module-05-interactive-ui.md` (900行)
- `content/modules/module-06-ui-customization.md` (1,048行)
- `content/modules/module-07-deployment.md` (1,242行)
- `content/README.md`

### 5. 実装済みファイル

#### `lib/markdown.ts`
**役割**: マークダウンファイルを読み込み、解析する

**提供する関数:**
- `getAllModules()`: 全モジュールの一覧を取得（order順にソート）
- `getModuleBySlug(slug)`: 特定のモジュールを取得

**返り値の型:**
```typescript
interface ModuleData {
  slug: string;         // 例: "module-01-environment-setup"
  title: string;        // 例: "モジュール1: 開発環境のセットアップ"
  duration: string;     // 例: "45-60分"
  difficulty: string;   // 例: "⭐☆☆☆☆"
  content: string;      // マークダウン本文
  order: number;        // 1-7（表示順序）
}
```

**注意点:**
- マークダウンから`**所要時間**:`と`**難易度**:`を正規表現で抽出
- `module-XX`の番号でorder（表示順序）を決定

#### `components/Sidebar.tsx`
**役割**: 左サイドバー（モジュール一覧表示）

**特徴:**
- Client Component（`'use client'`）
- 現在のページをハイライト（青色）
- 各モジュールに丸い番号バッジ
- `md:block`でモバイルでは非表示

**Props:**
```typescript
interface SidebarProps {
  modules: ModuleData[];
}
```

**デザイン:**
- 固定サイドバー（`fixed left-0 top-0`）
- 幅: 256px (`w-64`)
- アクティブ: `bg-blue-50 text-blue-700`
- 非アクティブ: `text-gray-700 hover:bg-gray-50`

#### `components/MarkdownRenderer.tsx`
**役割**: マークダウンをHTMLに変換して表示

**特徴:**
- Client Component（`'use client'`）
- `react-markdown`を使用
- カスタムコンポーネントでスタイリング
- シンタックスハイライト対応

**Props:**
```typescript
interface MarkdownRendererProps {
  content: string;
}
```

**スタイリング:**
- h1: `text-4xl font-bold text-gray-900`
- h2: `text-3xl font-bold text-gray-900`
- コードブロック: `bg-gray-900 text-gray-100`（highlight.js使用）
- インラインコード: `bg-gray-100 text-red-600`
- テーブル: ボーダー付き
- リンク: `text-blue-600 hover:text-blue-800 underline`

**highlight.jsのスタイル:**
```typescript
import 'highlight.js/styles/github.css';
```

---

## ⏳ 未実装の作業（これから実装が必要）

### 1. トップページ（`app/page.tsx`）

#### 必要な実装

**目的:**
- 講座の概要を表示
- 全モジュール一覧をカード形式で表示
- 「講座をはじめる」ボタン（第1モジュールへリンク）

**デザイン要件:**

##### ヘッダーセクション
```
┌─────────────────────────────────────┐
│     Engineer Course                 │
│     Next.jsポートフォリオサイト制作講座│
│     非エンジニアでも、動画も含めた     │
│     本格的なポートフォリオサイトが作れる│
└─────────────────────────────────────┘
```

- 中央揃え
- タイトル: `text-5xl font-bold`
- サブタイトル: `text-xl text-gray-700`

##### 講座の特徴セクション
**4つの特徴をグリッド表示（2x2）:**

1. **動画対応ポートフォリオ**
   - アイコン: 青色の丸（番号1）
   - 説明文

2. **UIカスタマイズ**
   - アイコン: 紫色の丸（番号2）
   - 説明文

3. **実践的な学習**
   - アイコン: 緑色の丸（番号3）
   - 説明文

4. **非エンジニア向け**
   - アイコン: オレンジ色の丸（番号4）
   - 説明文

**デザイン:**
- カード: `bg-white rounded-2xl shadow-xl p-8`
- アイコン: `w-12 h-12 rounded-lg flex items-center justify-center text-white font-bold text-xl`
- 色: `bg-blue-500`, `bg-purple-500`, `bg-green-500`, `bg-orange-500`

##### モジュール一覧セクション
**全7モジュールをカード形式で表示:**

各カード:
```
┌───────────────────────────────────────────┐
│ [1] モジュール1: 開発環境のセットアップ     │→│
│     45-60分  ⭐☆☆☆☆                       │
└───────────────────────────────────────────┘
```

- カード: `bg-white rounded-xl shadow-md hover:shadow-lg`
- 左ボーダー: `border-l-4 border-blue-500`
- 番号バッジ: `bg-blue-500 w-12 h-12 rounded-lg text-white font-bold`
- 右矢印アイコン: `text-blue-500`
- ホバーで影が濃くなる

##### CTAボタン
```
┌──────────────────┐
│  講座をはじめる    │
└──────────────────┘
```

- `bg-blue-600 hover:bg-blue-700 text-white`
- `font-bold text-lg px-8 py-4 rounded-lg`
- 第1モジュール（`/module/module-01-environment-setup`）へリンク

**必要なインポート:**
```typescript
import Link from 'next/link';
import { getAllModules } from '@/lib/markdown';
```

**実装例の骨格:**
```typescript
export default function Home() {
  const modules = getAllModules();

  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
      <div className="max-w-6xl mx-auto px-4 py-16">
        {/* ヘッダー */}
        {/* 講座の特徴 */}
        {/* モジュール一覧 */}
        {/* CTAボタン */}
      </div>
    </div>
  );
}
```

---

### 2. モジュールページ（`app/module/[slug]/page.tsx`）

#### 必要な実装

**目的:**
- 選択されたモジュールのマークダウンコンテンツを表示
- サイドバーでモジュール一覧を表示
- 前後のモジュールへのナビゲーション

**レイアウト:**
```
┌─────────┬─────────────────────────┐
│         │                         │
│ Sidebar │   メインコンテンツエリア  │
│         │                         │
│ (固定)  │   (スクロール可能)       │
│         │                         │
└─────────┴─────────────────────────┘
```

**デザイン要件:**

##### ページ構造
- サイドバー: `Sidebar`コンポーネント使用
- メインエリア: `md:ml-64`（サイドバー分の左マージン）

##### メインコンテンツエリア
```
┌─────────────────────────────────┐
│ [←前のモジュール] [次のモジュール→]│
├─────────────────────────────────┤
│                                 │
│  マークダウンコンテンツ           │
│  (MarkdownRendererで表示)        │
│                                 │
├─────────────────────────────────┤
│ [←前のモジュール] [次のモジュール→]│
└─────────────────────────────────┘
```

**前後ナビゲーションボタン:**
- 前のモジュール: `text-blue-600 hover:text-blue-800`
- 次のモジュール: `bg-blue-600 hover:bg-blue-700 text-white`
- 丸みのあるボタン: `rounded-lg px-6 py-3`
- 最初/最後のモジュールでは該当ボタンを非表示

**必要なインポート:**
```typescript
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { getAllModules, getModuleBySlug } from '@/lib/markdown';
import Sidebar from '@/components/Sidebar';
import MarkdownRenderer from '@/components/MarkdownRenderer';
```

**実装例の骨格:**
```typescript
export async function generateStaticParams() {
  const modules = getAllModules();
  return modules.map((module) => ({
    slug: module.slug,
  }));
}

export default function ModulePage({ params }: { params: { slug: string } }) {
  const module = getModuleBySlug(params.slug);

  if (!module) {
    notFound();
  }

  const modules = getAllModules();
  const currentIndex = modules.findIndex(m => m.slug === params.slug);
  const prevModule = currentIndex > 0 ? modules[currentIndex - 1] : null;
  const nextModule = currentIndex < modules.length - 1 ? modules[currentIndex + 1] : null;

  return (
    <div className="flex min-h-screen bg-gray-50">
      <Sidebar modules={modules} />
      <main className="flex-1 md:ml-64">
        {/* 上部ナビゲーション */}
        {/* マークダウンコンテンツ */}
        {/* 下部ナビゲーション */}
      </main>
    </div>
  );
}
```

---

### 3. レイアウトファイル（`app/layout.tsx`）

#### 必要な修正

**現在の状態:**
- デフォルトのGeistフォント使用
- `lang="en"`
- デフォルトのメタデータ

**必要な変更:**

```typescript
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Engineer Course - Next.jsポートフォリオ講座",
  description: "非エンジニア向けのNext.jsポートフォリオサイト制作講座",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ja">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
```

**変更点:**
1. フォント: `Geist` → `Inter`
2. 言語: `lang="en"` → `lang="ja"`
3. タイトル・説明文を日本語に

---

### 4. グローバルCSS（`app/globals.css`）

#### 追加が必要なスタイル

**現在:**
- Tailwind CSSのデフォルト設定のみ

**追加が必要:**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Proseスタイリング（マークダウン用） */
.prose {
  max-width: none;
}

.prose h1 {
  margin-top: 2rem;
  margin-bottom: 1rem;
}

.prose h2 {
  margin-top: 2rem;
  margin-bottom: 1rem;
  padding-bottom: 0.5rem;
  border-bottom: 2px solid #e5e7eb;
}

.prose h3 {
  margin-top: 1.5rem;
  margin-bottom: 0.75rem;
}

.prose pre {
  margin-top: 1rem;
  margin-bottom: 1rem;
  border-radius: 0.5rem;
  overflow-x: auto;
}

.prose code {
  font-size: 0.875em;
}

.prose table {
  margin-top: 1rem;
  margin-bottom: 1rem;
}

/* スムーススクロール */
html {
  scroll-behavior: smooth;
}

/* コードブロックのスタイリング改善 */
pre code {
  background: transparent !important;
  padding: 0 !important;
}
```

---

## 🎨 デザイン参考

### カラーパレット（Progate風）

**プライマリカラー:**
- 青: `bg-blue-500` (#3B82F6), `bg-blue-600` (#2563EB), `bg-blue-700` (#1D4ED8)
- 紫: `bg-purple-500` (#A855F7)
- 緑: `bg-green-500` (#22C55E)
- オレンジ: `bg-orange-500` (#F97316)

**背景:**
- メイン背景: `bg-gray-50` (#F9FAFB)
- カード背景: `bg-white` (#FFFFFF)
- コードブロック: `bg-gray-900` (#111827)

**テキスト:**
- メインテキスト: `text-gray-900` (#111827)
- サブテキスト: `text-gray-700` (#374151), `text-gray-600` (#4B5563)
- リンク: `text-blue-600` (#2563EB)

**注意:**
- グラデーション禁止（`from-*`, `to-*`, `via-*`は使わない）
- 単色のみ使用

---

## 🔧 重要な技術的注意点

### 1. App Routerの使用
- `app/`ディレクトリを使用（Pages Routerではない）
- Server Componentがデフォルト
- Client Componentには`'use client'`が必要

### 2. 動的ルート
- `app/module/[slug]/page.tsx`
- `generateStaticParams()`でビルド時に全ページを生成
- ISR（Incremental Static Regeneration）は不要

### 3. マークダウン処理
- `lib/markdown.ts`の関数を必ずServer Componentで呼ぶ
- `fs`はサーバーサイドでのみ動作
- Client Componentには処理済みのデータを渡す

### 4. Import Alias
- `@/`でルートディレクトリを参照
- 例: `import { getAllModules } from '@/lib/markdown'`

### 5. TypeScript
- 型エラーは必ず修正
- `any`の使用は最小限に

---

## 📝 実装チェックリスト

### 必須実装

- [ ] `app/page.tsx` - トップページ作成
  - [ ] ヘッダーセクション
  - [ ] 講座の特徴セクション（4つ）
  - [ ] モジュール一覧セクション（7つ）
  - [ ] CTAボタン

- [ ] `app/module/[slug]/page.tsx` - モジュールページ作成
  - [ ] `generateStaticParams()`実装
  - [ ] サイドバー配置
  - [ ] マークダウンコンテンツ表示
  - [ ] 前後ナビゲーション実装

- [ ] `app/layout.tsx` - レイアウト修正
  - [ ] フォント変更（Inter）
  - [ ] 言語変更（ja）
  - [ ] メタデータ変更

- [ ] `app/globals.css` - スタイル追加
  - [ ] Proseスタイリング
  - [ ] スムーススクロール
  - [ ] コードブロック改善

### 動作確認

- [ ] 開発サーバー起動（`npm run dev`）
- [ ] トップページ表示確認
- [ ] モジュールページ表示確認（全7ページ）
- [ ] サイドバーナビゲーション確認
- [ ] 前後ナビゲーション確認
- [ ] マークダウン表示確認
  - [ ] 見出し
  - [ ] コードブロック
  - [ ] テーブル
  - [ ] リスト
  - [ ] リンク
- [ ] レスポンシブ確認（モバイル/タブレット/PC）
- [ ] ビルド確認（`npm run build`）

---

## 🚀 次のステップ（実装後）

### 1. デプロイ準備
```bash
npm run build
# エラーがないか確認
```

### 2. Vercelデプロイ
```bash
vercel --prod
```

### 3. 拡張機能（任意）
- [ ] 進捗管理機能
- [ ] 検索機能
- [ ] ダークモード
- [ ] 目次自動生成（ページ内）
- [ ] コピーボタン（コードブロック）

---

## 📞 Claude Codeへの指示例

以下のように指示してください：

```
C:/portfolio-course/engineer-course/ で以下を実装してください：

【1. トップページ（app/page.tsx）】
- 講座タイトル「Engineer Course」とサブタイトルを中央配置
- 講座の特徴を4つ、2x2グリッドで表示（青・紫・緑・オレンジの丸いアイコン付き）
- 全7モジュールをカード形式で一覧表示（左に青ボーダー、番号バッジ、右矢印アイコン）
- 「講座をはじめる」ボタン（第1モジュールへリンク）
- カラフルだがグラデーション禁止、絵文字禁止

【2. モジュールページ（app/module/[slug]/page.tsx）】
- generateStaticParams()で全モジュールの静的生成
- 左サイドバー（Sidebarコンポーネント使用）
- メインコンテンツエリア（MarkdownRendererで表示）
- 上下に前後ナビゲーションボタン
- 最初/最後のモジュールでは該当ボタンを非表示

【3. レイアウト修正（app/layout.tsx）】
- Interフォントに変更
- lang="ja"に変更
- メタデータを日本語に変更

【4. スタイル追加（app/globals.css）】
- Proseスタイリング（マークダウン用）
- スムーススクロール
- コードブロックの改善

【使用する既存ファイル】
- lib/markdown.ts の getAllModules(), getModuleBySlug()
- components/Sidebar.tsx
- components/MarkdownRenderer.tsx

【デザイン要件】
- Progate風（クリーン、読みやすい）
- グラデーション禁止（単色のみ）
- 絵文字禁止
- カラフル（青・紫・緑・オレンジ）
- レスポンシブ対応

実装後、npm run dev で動作確認してください。
```

---

**引き継ぎ完了。実装を開始してください。**
